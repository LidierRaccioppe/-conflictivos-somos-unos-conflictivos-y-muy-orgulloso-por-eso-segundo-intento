public class Main {
        public static void main(String[] args) {
                //el lider (absoluto): crear projecto, subir repo a github, añade al colaborador, el lider (todopoderoso) crea varios commits en la rama main
                //el colaborador (la plebe): clona el proyecto/ repo, crea la rama colab, hace commits (en la main), sube la rama al github
                //lider (magistral): hace un pullrequest en la rama colab, un merge en /conflict, subir resultado
        }
}